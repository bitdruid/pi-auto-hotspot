[Unit]
Description=run hotspot on boot and check for known networks in range

[Service]
ExecStart=hotspot --auto
#ExecStartPre=hotspot --auto
#ExecStart=hotspot --join

[Install]
WantedBy=multi-user.target